import { loadCoinDetail } from '../api/coingecko.js';
import { fmt } from '../utils/format.js';

export function initCoinModal() {
  // Здесь логика открытия модалки и заполнения данными
  // (по аналогии с твоим монолитным кодом)
}
